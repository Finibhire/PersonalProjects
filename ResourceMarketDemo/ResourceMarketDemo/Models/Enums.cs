using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace ResourceMarketDemo.Models
{
    public enum CurrencyType
    {
        DragonPoints = 1, Gold = 2, HyperCoin = 3, GoldPiecesCoin = 4, HTML5Coin = 5, FLAP = 6
    }

    public enum ResourceType
    {
        Wood = 1, Fish, Stone, Iron
    }
}